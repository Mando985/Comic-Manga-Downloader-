[project]
name = "comic-downloader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "madhav", email = "madhavraj1212@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "img2pdf>=0.6.3",
    "pillow>=12.3.0",
    "requests>=2.34.2",
    "scrapy>=2.17.0",
    "textual>=8.2.8",
    "textual-image>=0.13.2",
]

[project.scripts]
comic = "comic_downloader.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
